# 架构决策记录

架构决策记录（ADR）用于保存重要且难以逆转的技术选择及其背景。

## 状态

已接受：

1. [ADR-0001：macOS 首发平台](0001-macos-first.md)。

其余记录将在架构验证中按以下顺序建立：

2. 桌面应用壳。
3. 本地 API/IPC 协议。
4. 备份仓库和快照格式。
5. 变更检测、哈希与去重粒度。
6. 调度和系统服务集成。
7. WebDAV/S3 目标抽象与一致性策略。
8. [加密、打包与压缩管线](0008-data-transformation.md)（已接受，参数待验证）。
9. [文件筛选规则语义](0009-file-selection.md)（拟议）。

## 模板

每份记录包含：标题、日期、状态、背景、决策、备选方案、后果和验证方式。文件命名采用 `NNNN-short-title.md`。
