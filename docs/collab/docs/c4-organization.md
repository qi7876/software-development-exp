# docs c4 organization 分支记录

## 开发意图

按照项目协作约定，将架构资料迁入 C4 目录，补齐 C1 和部署视图，建立本地 CI，并保护课程报告的手工封面格式。

## 进度

- [x] 从最新 `origin/main` 创建短期分支。
- [x] 将逻辑设计的单一事实来源和生成物迁入 `docs/c4/`。
- [x] 增加 System Context 与 Deployment，规范现有 C2、C3 标识。
- [x] 建立主协作文档和本分支文档。
- [x] 删除报告生成器的全局字体归一化行为。
- [x] 增加报告保护区回归测试和统一本地 CI。
- [x] 完成本地 CI、PlantUML 图像检查和 DOCX 保护区回归验收。

## 设计决策

- 不维护 C4 Code 图，现有类图作为逻辑模型和课程交付物保留。
- 当前没有更大范围的多系统景观，不创建 System Landscape。
- C1 和部署图进入 C4 文档；课程报告继续保留原有两张构件图、两张类图和三张顺序图，避免改变教师模板结构。
- 不添加远程 CI，不推送分支，不配置自动部署。

## 后续计划

验证完成后以一个 `docs:` 提交交付。本分支不实现产品代码；下一分支应从架构探针或最小 Rust 核心的先失败测试开始。

## 验证结果

本地 CI 全部通过，9 张 PlantUML 图成功编译并完成图像检查。DOCX 自动渲染器因环境未安装 LibreOffice 无法执行；已通过 Microsoft Word 确认封面保持原样，并由自动化测试验证同步前后“需求分析说明书”之前的段落 OOXML 完全一致。完整分页效果由项目维护者继续在 Microsoft Word 中复核。
